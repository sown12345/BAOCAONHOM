<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.BAOCAONHOM"
        tools:targetApi="31">

        <activity android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" />
        <activity android:name=".Admin.AdminHomeActivity" />
        <activity android:name=".Admin.Category.ManageCategoriesActivity" />
        <activity android:name=".Admin.Product.ManageProductsActivity" />
        <activity android:name=".Admin.Product.ManageProductImagesActivity" />
        <activity android:name=".Admin.Product.ManageProductVariantsActivity" />
        <activity android:name=".Admin.Product.AddEditProductActivity" />
        <activity android:name=".Admin.Category.AddEditCategoryActivity" />


        <activity android:name=".Customer.home.UserHomeActivity" />
        <activity android:name=".Customer.product.ProductDetailActivity" />
        <activity android:name=".Customer.cart.CartActivity" />
        <activity android:name=".Customer.checkout.CheckoutConfirmActivity" />
        <activity android:name=".Customer.orderdetail.OrderDetailActivity" />
        <activity android:name=".Customer.order.OrderHistoryActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".Customer.Profile.ProfileActivity" />

    </application>

</manifest>